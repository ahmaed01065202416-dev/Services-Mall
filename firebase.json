{
  "_comment": "This project is hosted on Cloudflare Pages, NOT Firebase Hosting. This file exists only so `firebase deploy --only firestore:rules,database` can push the Firestore/Realtime-Database security rules below — there is no 'hosting' block here on purpose, since Firebase Hosting is never used.",
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "database": {
    "rules": "database.rules.json"
  }
}
